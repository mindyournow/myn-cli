package screens

// TaskDetailOverlay is a lightweight helper that renders a task detail panel
// on top of an existing screen view. The actual rendering is done by
// renderTaskDetail in styles.go and is called directly by screens that support
// Enter=detail (now, inbox, tasks).
